import { OverflowClassDirective } from './overflow-class.directive';

describe('OverflowClassDirective', () => {
  it('should create an instance', () => {
    const directive = new OverflowClassDirective();
    expect(directive).toBeTruthy();
  });
});
